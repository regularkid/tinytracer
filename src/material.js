class Material
{
    constructor(ambient, diffuse, spec, specExponent)
    {
        this.ambient = ambient;
        this.diffuse = diffuse;
        this.spec = spec;
        this.specExponent = specExponent;
    }
}